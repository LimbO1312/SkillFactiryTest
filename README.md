Библиотеки, которые необходимо установить
pip install pandas
pip install lxml
pip install openpyxl

Скачать репозиторий, запустить командой python Parser.py
"вести данные" - необходимо вбить числовую последовательность, уникальную для профиля, ее можно увидеть и скопировать в Address bar
